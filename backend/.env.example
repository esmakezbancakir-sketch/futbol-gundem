API_TOKEN=change-me-to-a-random-secret
# Claude CLI auth: a long-lived token from `claude setup-token` (subscription-based,
# no separate Anthropic API key needed). The claude binary reads this itself.
CLAUDE_CODE_OAUTH_TOKEN=
RETENTION_DAYS=30
SCRAPE_INTERVAL_HOURS=6
MAX_TOPICS_PER_RUN=10
